# Uncomment the next line to define a global platform for your project
platform :ios, '13.0'

target 'PodcastKit' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PodcastKit
  pod 'Alamofire'
  pod 'Kingfisher', '~> 7.0'
  pod 'IQKeyboardManagerSwift'
  pod 'FeedKit', '~> 9.0'
  
end
